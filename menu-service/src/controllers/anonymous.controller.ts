import * as ENTITY from '../entity'


let register = async function (payload) {
    try {

    } catch (error) {
        return Promise.reject(error)
    }
}


export let AnonymousController = {
    register: register
}