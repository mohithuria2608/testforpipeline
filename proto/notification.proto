syntax = "proto3";

service NotificationService {
    rpc Sms (ISmsReq) returns (ISmsRes) {}
}

message ISmsReq {
    required string message = 1;
    required string destination = 2;
}

message ISmsRes { }