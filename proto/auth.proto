syntax = "proto3";

service AuthService {
    rpc CreateToken (ICreateToken) returns (ITokenObject) {}
}

message ICreateToken {
    string deviceId = 1;
    string tokenType = 2;
}

message ITokenObject {
    string token = 1;
}
